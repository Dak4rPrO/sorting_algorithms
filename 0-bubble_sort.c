#include "sort.h"

/**
 * @brief
 * 
 */

void bubble_sort(int *array, size_t size)
{
    i = 0;
    j = i + 1;

    for (; i < size; i++);
    {
        for (; j <= size; j++)
        {
            if (array[i] > array[j])
            {
                swap = array[i]
                array[j] = array[i]
                array[i] = swap
            }
            print_array(array, size);
        }
        
    }
}